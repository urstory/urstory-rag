# =============================================================================
# UrstoryRAG 프로덕션 환경변수 (.env.prod.example)
# =============================================================================
# 이 파일을 .env로 복사한 후 모든 값을 프로덕션에 맞게 변경하세요.
# "changeme" 또는 빈 값이 남아있으면 서비스가 안전하지 않습니다.
#
#   cp .env.prod.example .env
#   vim .env
# =============================================================================

# ---------------------------------------------------------------------------
# 인프라: PostgreSQL (pgvector)
# ---------------------------------------------------------------------------
POSTGRES_USER=rag_prod
POSTGRES_PASSWORD=            # 필수! 강력한 비밀번호 설정
POSTGRES_DB=rag_production
POSTGRES_PORT=5432

# ---------------------------------------------------------------------------
# 인프라: Elasticsearch
# ---------------------------------------------------------------------------
ES_PORT=9200

# ---------------------------------------------------------------------------
# 백엔드: 데이터베이스 연결
# ---------------------------------------------------------------------------
# Docker 내부에서는 호스트명이 shared-postgres (infra/docker-compose.yml의 container_name)
# 외부 관리형 DB 사용 시 해당 호스트로 변경
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@shared-postgres:5432/${POSTGRES_DB}

# ---------------------------------------------------------------------------
# 백엔드: 서비스 연결
# ---------------------------------------------------------------------------
ELASTICSEARCH_URL=http://shared-elasticsearch:9200
REDIS_URL=redis://shared-redis:6379

# ---------------------------------------------------------------------------
# 인증 (필수)
# ---------------------------------------------------------------------------
# JWT 시크릿 키 - 반드시 생성: openssl rand -hex 32
JWT_SECRET_KEY=

# 최초 관리자 계정 (서버 시작 시 users 테이블이 비어있으면 자동 생성)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=               # 필수! 기본값 사용 금지

# 일반 사용자 회원가입 허용 여부
ALLOW_PUBLIC_SIGNUP=false

# CORS 허용 도메인 (콤마 구분)
CORS_ORIGINS=https://your-domain.com

# API 속도 제한
RATE_LIMIT_ENABLED=true

# ---------------------------------------------------------------------------
# 로깅
# ---------------------------------------------------------------------------
LOG_LEVEL=WARNING
LOG_FORMAT=json

# ---------------------------------------------------------------------------
# Sentry 에러 추적 (권장)
# ---------------------------------------------------------------------------
SENTRY_DSN=                   # https://xxx@sentry.io/yyy
SENTRY_ENVIRONMENT=production
# SENTRY_TRACES_SAMPLE_RATE=0.1  # 기본값 10%

# ---------------------------------------------------------------------------
# 외부 API 키 (필수)
# ---------------------------------------------------------------------------
OPENAI_API_KEY=               # 임베딩 + LLM 생성에 사용
ANTHROPIC_API_KEY=            # 선택

# ---------------------------------------------------------------------------
# Langfuse v3 관찰성 (권장)
# ---------------------------------------------------------------------------
# Langfuse UI에서 프로젝트 생성 후 API 키 입력
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://langfuse-web:3000

# Langfuse 인증 시크릿 - 반드시 생성: openssl rand -hex 32
NEXTAUTH_SECRET=
SALT=

# Langfuse 암호화 키 (64자 hex) - 반드시 생성: openssl rand -hex 32
LANGFUSE_ENCRYPTION_KEY=

# ---------------------------------------------------------------------------
# ClickHouse (Langfuse 분석용)
# ---------------------------------------------------------------------------
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=          # 필수! 기본값 사용 금지

# ---------------------------------------------------------------------------
# Langfuse Redis
# ---------------------------------------------------------------------------
LANGFUSE_REDIS_AUTH=          # 필수! 기본값 사용 금지

# ---------------------------------------------------------------------------
# MinIO (Langfuse S3 호환 스토리지)
# ---------------------------------------------------------------------------
MINIO_ROOT_USER=              # 필수! 기본값(minioadmin) 사용 금지
MINIO_ROOT_PASSWORD=          # 필수! 기본값(minioadmin) 사용 금지

# ---------------------------------------------------------------------------
# Docker 이미지 태그
# ---------------------------------------------------------------------------
TAG=latest                    # 또는 특정 커밋 SHA (예: abc1234)
